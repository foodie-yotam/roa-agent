name: builder_supervisor
type: supervisor

description: |
  Builder team supervisor for development tools.

system_prompt_file: prompts/supervisors/builder_supervisor.md

workers:
  - name: dev_tools
    agent_config: agents/dev_tools_agent.yaml

routing_config:
  show_tool_table: true
